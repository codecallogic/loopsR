<navbar class="navbar navbar-expand-sm navbar-light bg-light">
    <div class="container-fluid">
      <a href="/" class="navbar-brand">Nav</a>
      <ul class="navbar-nav">
        <li class="nav-item">
          <% if (user){ %>
            <form action="/boards/search" method="POST" class="form-inline ml-auto">
              <input type="text" class="form-control mr-2" name="query" placeholder="Enter artist, album, song name">
              <input type="hidden" name="id" value="<%= user.googleId %>">
              <button class="search">Search</button>
            </form>
          <% }else{ %>

          <% } %>
        </li>
        <li class="nav-item">
          <% if (user){ %>
            <a href="/boards/form">Post</a>
          <% }else{ %>

          <% } %>
        </li>
        <li class="nav-item">
          <% if (user){ %>
           
            <a href="/boards/dashboard">Dashboard</a>
          <% }else{ %>

          <% } %>
        </li>
        <li class="nav-item">
            <% if (user) { %>
            <a href="/logout">Logout</a>
          <% } else { %>
            <a href="/auth/google">Login</a>
          <% } %>
        </li>
      </ul>
    </div>
  </navbar>