<%- include('../partials/header') %>
<%- include('../partials/navbar') %>
    <div class="container-fluid">
        <div class="row">
            <div class="col-2"></div>
            <div class="col-10">
                <table class="table table-hover table-striped text-center">
                    <thead>
                        <th>Post</th>
                        <th>Edit</th>
                        <th>Delete</th>
                    </thead>
                    <tbody>
                        <% board.forEach( b => {  %>
                            <tr>
                                <td><a href="/boards/<%= b._id %>/post" target="_blank" rel="noopener noreferrer" style="text-decoration: none;"><%= b.title %></a></td>
                                <td><a href="/boards/<%= b._id %>/edit" class="btn" style="background-color:#FDEEC0!important;">Edit</a></td>
                                <td><a href="/boards/<%= b._id %>/delete" class="btn btn-danger">Delete</a></td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
<%- include('../partials/footer') %>